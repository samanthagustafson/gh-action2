Cookie var;
var.setHttpOnly(false);

session.getCookie().setHttpOnly(false);

myCookie.setHttpOnly(false);

getCookie("sessionID", config)
. setHttpOnly (  false  );

//GOOD CODE
var1.setHttpOnly(false);

session.getCookie().setHttpOnly(true);