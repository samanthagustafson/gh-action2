HttpCookie var;
var.setSecure(false);

session.getCookie().setSecure(false);

myCookie.setSecure(false);

getCookie("sessionID", config)
. setSecure (  false  );

//GOOD CODE
var1.setSecure(false);

session.getCookie().setSecure(true);
